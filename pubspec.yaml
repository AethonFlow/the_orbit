name: ri_orbit
description: Resonant Intelligence Engine - TheOrbit Core Domain (v0.1.0 - Erster Atemzug)
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # Pure mathematics - no external frameworks!

dev_dependencies:
  test: ^1.24.0
